
/**
 * Modules are great, but only the single default export.
 * 
 * This makes it easy to find what your module is actually
 * exporting and gives all modules the same interface to
 * interact with.
 */
const myModule = {

};

export default myModule;
